filebeat.inputs:
  - type: filestream
    enabled: true
    paths:
      - /app/logs/app.log

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]

setup.kibana:
  host: "http://kibana:5601"

processors:
  - add_docker_metadata: ~
